# Ticket compatibility

The serialized `is_closed` field is consumed by an older reporting service outside this repository. Removing it requires a coordinated migration. The three active-ticket checks are owned by the ticket lifecycle domain and are intended to change together.
