# Ticket lifecycle

Assignment and active-queue visibility were originally described as using the same "active ticket" policy. The reason archived tickets remain visible is undocumented, and it is not known whether that behavior is intentional compatibility or a latent bug.
